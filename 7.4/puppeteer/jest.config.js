module.exports = {
  //указывает на то, что каждый тест будет показан в отчете о процессе запуска
  verbose: true,
  //указываем, что будем использовать эту библиотеку
  preset: "jest-puppeteer",
  //указываем таймаут
  //testTimeout: 60000,
};
