{
  "name": "intro",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "electron_headless": "npx cypress run --browser electron",
    "chrome_headless": "npx cypress run --browser chrome",
    "electron_headed": "npx cypress run --headed --browser electron",
    "chrome_headed": "npx cypress run --headed --browser chrome",
    "electron_headless_env": "npx cypress run --browser electron --config-file cypress.env.json",
    "chrome_headless_env": "npx cypress run --browser chrome --config-file cypress.env.json",
    "electron_headed_env": "npx cypress run --headed --browser electron --config-file cypress.env.json",
    "chrome_headed_env": "npx cypress run --headed --browser chrome --config-file cypress.env.json"
  },
  "author": "shadrin",
  "license": "ISC",
  "dependencies": {
    "cypress": "^9.5.2",
    "cypress-file-upload": "^5.0.8"
  }
}
